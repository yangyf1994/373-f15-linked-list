#ifndef PRINT_LIST_H
#define PRINT_LIST_H

#include "list.h"

// Takes a pointer to the head of a list and prints it.
// The list must be valid (head cannot be NULL)
void print_list(list_t* head);

#endif
